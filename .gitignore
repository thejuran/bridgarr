# Local-only planning/orchestration state (never published — see design spec)
.planning/
docs/superpowers/
.turingmind/
.orchestrator.json
bridgarr-brief.md

# Node
node_modules/
dist/

# Runtime data dir (settings.json holds API keys) — cwd-relative default DATA_DIR
data/

# macOS
.DS_Store
