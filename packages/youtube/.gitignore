# Package-local concerns only. The monorepo-root .gitignore owns tool-state
# (.planning/, .orchestrator.json, .turingmind/, docs/superpowers/) and the
# node_modules/dist/data/ rules; those standalone-repo entries can never match
# under packages/youtube and were trimmed during the bridgarr import.
node_modules/
dist/
data/
*.log
.DS_Store
